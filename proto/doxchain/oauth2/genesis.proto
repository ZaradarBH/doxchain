syntax = "proto3";

package beheroes.doxchain.oauth2;

import "gogoproto/gogo.proto";
import "doxchain/oauth2/params.proto";
import "doxchain/oauth2/device_code_registry.proto";
import "doxchain/oauth2/access_token_registry.proto";

option go_package = "github.com/be-heroes/doxchain/x/oauth2/types";

// GenesisState defines the oauth module's genesis state.
message GenesisState {
           Params       params           = 1 [(gogoproto.nullable) = false];
  repeated DeviceCodeRegistry  DeviceCodeRegistryList  = 2 [(gogoproto.nullable) = false];
  repeated AccessTokenRegistry accessTokenRegistryList = 3 [(gogoproto.nullable) = false];
}

